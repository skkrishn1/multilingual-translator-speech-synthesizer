# Direct dependencies only, pinned to the versions this project was built and tested against.
# Transitive packages are left to the resolver so the file stays portable between the local
# Python 3.14 environment and Streamlit Cloud's 3.13 (see runtime.txt).

streamlit==1.61.1
google-genai==2.17.0
gTTS==2.5.4
pypdf==6.15.0
pandas==3.0.5
openpyxl==3.1.5
python-dotenv==1.2.2

# Testing only — harmless on the deployed app, and keeps the suite runnable there.
pytest==9.1.1
